# Guidelines for AI Agents (Claude Code)

This file is the Claude Code entry point. Claude Code does not always
discover `AGENTS.md` automatically, so the complete shared guidance is
imported below; treat it as if it were written directly in this file.

@AGENTS.md

For the IDD entry path and phase routing, also read
[`docs/idd-workflow.md`](docs/idd-workflow.md).

Claude Code already follows the imported guidance's pause-and-ask rule
through its own interaction model. Use that model when a risk or
uncertainty requires user input; do not duplicate shared project rules
here.
