import type { Component } from 'solid-js';

/**
 * The top page.
 * @returns The component.
 */
const Index: Component = () => (
  <h1 class="bg-primary text-primary-content">Hello, world!</h1>
);

export default Index;
